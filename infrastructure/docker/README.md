# Docker

Container build assets for the SARATHI monorepo.

Files:
- service.Dockerfile: reusable FastAPI service image
- api_gateway.Dockerfile: gateway image
- python-service.Dockerfile: shared FastAPI service image for local development
- nginx.conf: reverse proxy sample
